{
  "name": "sarok1-viewer",
  "private": true,
  "scripts": {
    "build": "esbuild --bundle --format=esm --platform=browser --outfile=dxf.bundle.js bundle-entry.js",
    "dev": "npm run build && npx serve ."
  },
  "dependencies": {
    "dxf": "^5.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
